<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pk.dao.reservationDao">
    
    <insert id="insertReservation" parameterType="rservationDto">
        INSERT INTO reservation (userid, usertel, rtCode, rtAddr1, rtAddr2, rtTel, rtName, resNum, resTime, resDay, resKeyword)
        VALUES (#{userid}, #{usertel}, #{rtCode}, #{rtAddr1}, #{rtAddr2}, #{rtTel}, #{rtName}, #{resNum}, #{resTime}, #{resDay}, #{resKeyword})
    </insert>

    
</mapper>
